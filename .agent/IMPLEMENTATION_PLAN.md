# Audit Remediation Implementation Plan

> Cross-session tracker for fixing everything found in the 2026-07-08 full logic/system audit
> (see the "Bottom line" / per-package findings that were folded into `.agent/PROJECT_STATUS.md`
> and the strategic-initiatives section there). This file tracks **phase-by-phase execution
> status** so a new session can pick up exactly where the last one left off.
>
> Workflow per phase: implement → build → `git push` → SSH to the Pi (`ian@100.66.77.72`,
> workspace `~/dev/smart_chess_board`) → `git pull` → rebuild the affected package(s) →
> live-verify → update this file + `PROJECT_STATUS.md` → move to the next phase.

## Phase status

| Phase | Scope | Status |
|---|---|---|
| 1 | Coordinate/config fix (`board_map.yaml` + `x_max_mm` reconciliation) | ✅ done 2026-07-08 |
| 2 | `game_manager_node` permanent-death & correctness bugs | ✅ done 2026-07-08 |
| 3 | Safety: blocking executors defeat e-stop; action double-terminal bug; test-runner interlock | ✅ done 2026-07-08 |
| 4 | First live-motor session + remaining `gantry_control` findings (castling snapshot, corner-BFS start, physical calibration) | ⬜ not started (needs the user live for real motor motion — see `hardware-test.md`'s Phase 4 checklist) |
| 5 | `chess_perception` fixes | ✅ done 2026-07-09 |
| 6 | `chess_ui` fixes (e-stop clear, live param push, SSE queue, jog timeout) | ✅ done 2026-07-09 |
| 7 | Corner-visibility + manual-override UI (folded into Phase 8e) | ✅ done 2026-07-09 |
| 8a | Vision robustness: lock camera exposure/white-balance | ✅ done 2026-07-09 |
| 8b | Vision robustness: stability-gated post-move capture | ✅ done 2026-07-09 |
| 8c | Vision robustness: perspective-aware ROI + multi-modal (color+structure) scoring | ✅ done 2026-07-09 (mechanism; real-board ROI-bias tuning still needs the user) |
| 8d | Vision robustness: confidence-scored legal-move matching | ✅ done 2026-07-09 |
| 8e | Vision robustness: remaining developer tuning UI + corner-visibility tie-in | ✅ done 2026-07-09 (tuning log deferred, see notes) |
| 9a | Reconcile `full_system_launch.py`/`perception_launch.py` camera-config divergence | ✅ done 2026-07-09 |
| 9b | Game-state persistence/resume across crash or restart | ✅ done 2026-07-09 |
| 9c | Autostart-on-boot systemd unit + scoped perception/UI respawn | ✅ done 2026-07-09 (unit installed as a template only, not enabled — see notes) |
| 9d | Manual game-state override ("Advanced" tab) | ✅ done 2026-07-09 |
| 9e | Promotion choice UI (human under-promotion + computer piece text) | ✅ done 2026-07-09 |
| 9f | UX polish: invalid-move banner, engine-fallback indicator, clock node health checks | ✅ done 2026-07-09 |
| 9g | Draw-offer flow (simple human-declared draw) | ✅ done 2026-07-09 |
| 9h | Cleanup: graveyard reset on abort, stale `board_corners.json` removal | ✅ done 2026-07-09 |

## Phase details

### Phase 1 — Coordinate/config fix
**Files:** `src/gantry_control/config/board_map.yaml`, `gantry_kinematics_node.py`, `homing_node.py`

- `board_map.yaml` documented the old (pre-fix) coordinate convention while `motion_planner_node.py`'s code implements the new one — 5 of 8 files were unreachable (e2e4 rejected).
- `board_edge_safe_x_mm` (read by code) vs `board_edge_left_x_mm`/`board_edge_right_x_mm` (yaml) name mismatch — yaml value silently ignored.
- `x_max_mm` disagreed 3 ways: `homing_node` default 240, yaml/`gantry_kinematics_node` 250, `gantry_kinematics_node.py`'s own fallback 300.

**Fix:** rewrite yaml to match the code's own already-correct convention/defaults, rename the edge param, add `homing_node`'s `x_max_mm` to yaml, fix the stray 300 fallback.

**Verification:** `ros2 param get` on the Pi for all three nodes — no live motor movement (deferred to Phase 3/4 for safety).

**Status: ✅ DONE (2026-07-08).** Commit `0c4d8df` pushed to `ros-dev`, pulled and rebuilt on the Pi (`colcon build --symlink-install --packages-select gantry_control`, clean, 5.7s). Live-verified: launched `gantry_kinematics_node` and `motion_planner_node` standalone (no pigpio dependency) with `board_map.yaml`, confirmed via `ros2 param get` — `x_max_mm=250.0`, `y_max_mm=250.0`, `board_origin_x_mm=20.0`, `board_origin_y_mm=20.0`, `board_edge_safe_x_mm=230.0`, `graveyard_origin_x_mm=230.0`, `square_size_mm=25.0`. Hand-checked bounds: e2/e4 (col 4) → x=120mm, h1/h8 (col 7) → x=195mm, both now within `[0, 250]` (h-file was previously rejected at 375mm). Verification processes were killed afterward; the 5 pre-existing production nodes (`camera_node`, `board_detector_node`, `piece_detector_node`, `chess_os`, `test_runner_node`) were untouched throughout.
`homing_node`'s corrected `x_max_mm` param was **not** live-verified — `pigpiod` isn't running on the Pi and `sudo -n pigpiod` confirmed it needs an interactive password not available this session. Verified via static code/yaml inspection only; revisit when pigpiod is available (Phase 3/4).

### Phase 2 — `game_manager_node` fixes
**File:** `src/chess_logic/chess_logic/nodes/game_manager_node.py`

Homing-failure and every game-end path currently `break`/`return` out of the daemon game-loop thread permanently, killing all future `/game/*` service calls silently (services stay registered and lie about success). Plus: `/game/resign` races the loop thread, a failed computer move is pushed to the board model anyway (desync), stale perception/motion completions aren't correlated to the request that caused them, `PROMOTION_WAIT` has no timeout/flag-poll, and clock-hit servo failures are silently discarded.

**Verification:** ROS-graph-level only (service calls + synthetic topic publishes), no hardware required.

**Status: ✅ DONE (2026-07-08).** Commit `0485e3a` pushed to `ros-dev`, pulled and rebuilt on the Pi (`colcon build --symlink-install --packages-select chess_logic`, clean). Verified live using a throwaway stub node (`/tmp/stub_hw_services.py`, deleted after use — not committed) providing fake `/gantry/home`, `/camera/capture`, `/perception/capture_premove`, `/clock/hit`, and `/chess_engine/request_move` services, driven via `ros2 topic pub`/`ros2 service call`:
- **Homing-retry fix**: with `/gantry/home` genuinely unavailable (stub killed), the node cycled `HOMING → HOMING_FAILED → HOMING` every ~15s for 35+ seconds (3 full cycles) without the process ever dying — this is the exact scenario the audit found killed the game loop permanently.
- **Full turn cycle**: drove a complete human move (e2e4, validated via synthetic `/perception/changed_squares`) → engine reply (stub returned a real legal move via python-chess) → motion execution (`/motion/done`) → clock hit → back to `WAITING_PLAYER_MOVE`, all correctly sequenced.
- **The exact audit-reproduced bug**: called `/game/resign` (correctly ended the game, published the result) then `/game/new_game` — confirmed the game genuinely reset to `IDLE` and responded to a fresh clock press, unlike the audit's original finding where `/game/new_game` returned `success=True` but did nothing.
- **`MOTION_ERROR` path**: simulated a failed move (`/motion/done` with `data:false`) — confirmed the move was *not* pushed to the board model, the node transitioned to `MOTION_ERROR` and paused (didn't push a stale/wrong position), and `/game/new_game` recovered it back to `IDLE`.
- **Awaiting-gate fix**: repeated/late `/perception/changed_squares` and `/motion/done` publishes outside their intended wait windows were correctly logged as "ignoring — not currently awaiting" and had no effect, confirming stale-event correlation works.
- One false alarm during testing: `game_manager_node`'s stdout is block-buffered when redirected to a file, making log-only observation initially look like clock-hit wasn't received — resolved by relaunching with `PYTHONUNBUFFERED=1`; not a code bug.
- All verification processes and the stub script were cleaned up afterward; the 5 pre-existing production nodes were confirmed untouched throughout.

### Phase 3 — Safety fixes (gate before live motor testing)
**Files:** `homing_node.py`, `gantry_kinematics_node.py`, `stepper_driver_node.py`, `servo_node.py`, `clock_servo_node.py`, `test_runner_node.py`/`test_runner.py`/`base_test.py`/`run_hw_test.sh`, new `chess_hw_interface/gpio_lock.py`

Single-threaded executors + blocking move/home callbacks mean `/emergency_stop` (and, for `gantry_kinematics_node`, its own `/gantry/pose` feedback) can't be processed mid-motion. Fix via `MultiThreadedExecutor` + callback groups. Also fixes `gantry_kinematics_node`'s double-terminal-goal-state bug on cancel, hardware-test cancellation not reaching the root-owned subprocess, and no GPIO interlock between a live game and a running hardware test.

**Status: ✅ DONE (2026-07-08).** Commits `15cac76` (main fix) and `9ff54a0` (follow-up: `cancel_check` constructor-forwarding fix found during live verification), pushed to `ros-dev`, pulled and rebuilt on the Pi (`chess_hw_interface`, `gantry_control`).

- **Blocking-executor fix**: `homing_node`, `stepper_driver_node`, `servo_node`, `clock_servo_node`, `gantry_kinematics_node` all switched from the default single-threaded `rclpy.spin()` to `MultiThreadedExecutor(num_threads=4)`, with `/emergency_stop` (and, for `gantry_kinematics_node`, `/gantry/pose` + limit-switch subscriptions) moved into their own callback group separate from the blocking service/action-execute callback. Verified live and empirically: launched `gantry_kinematics_node` standalone (no pigpio dependency) and drove a real `/gantry/move` action goal while an external script published synthetic `/gantry/pose` feedback concurrently — the goal correctly detected arrival and received 28 feedback messages *while* `execute_callback` was blocked in its polling loop, proving the pose subscription now runs concurrently instead of being starved. Under the old single-threaded code this would have hung forever (arrival can never be detected if pose can never update).
  - **Update (same day, follow-up session once `pigpiod` was started)**: `homing_node`, `stepper_driver_node`, `servo_node`, `clock_servo_node` all launched standalone against a real `pigpiod` and connected cleanly with no errors — closing the runtime-verification gap noted above. `stepper_driver_node` and `homing_node` were run **simultaneously**, confirming their shared-lock coexistence (see GPIO interlock below) works for real, not just in the unit test. No motors were commanded to move — this only confirms clean startup/pigpio-connection under the new executor/callback-group code; actual `/emergency_stop`-during-motion (real motor movement) is deferred to Phase 4's first live-motor session.
- **`gantry_kinematics_node` double-terminal-goal-state fix**: `_execute_trapezoidal_move` no longer calls `goal_handle.canceled()` itself — it returns a `'completed'`/`'canceled'` status string, and `execute_callback` is now the sole caller of `succeed()`/`abort()`/`canceled()`. Verified live: sent a slow move goal, canceled it mid-flight, confirmed a single clean `CANCELED` terminal state (status 5) with no exception in the node's log (previously this path called two terminal-state methods on the same goal handle, the second of which raises uncaught).
- **Hardware-test cancellation**: root cause was that `test_runner_node` (running as `ian`) calling `os.killpg()`/SIGTERM against the test subprocess is silently rejected by the kernel once that subprocess re-execs as root via `sudo` (see `setup/smart-chess-hw-tests.sudoers`) — an unprivileged process can never signal a more-privileged one, regardless of process group. Added a killswitch-file mechanism instead: `test_runner_node` creates a unique cancel file and passes its path through `run_hw_test.sh`'s existing `sudo VAR=val` env-forwarding (`SETENV` is already in the sudoers rule); `test_runner.py` and `base_test.py`'s `HardwareTest.run()` poll for it between test steps/subtests and exit cleanly. Verified live: a synthetic `HardwareTest` subclass run through the real installed `base_test.py` correctly ran all steps when no cancellation was requested, and stopped after exactly one step when the cancel file appeared mid-run. Also verified the env-var → `cancel_check` wiring in `test_runner.py` directly. **Not verified**: the actual sudo hop itself, end-to-end through `run_hw_test.sh` — this environment has no passwordless sudo and no `CHESS_SUDO_PASS` set (same limitation noted in Phase 1), so the real subprocess can't be exercised. The forwarding syntax is identical to the already-proven-working `PYTHONPATH`/`LD_LIBRARY_PATH` forwarding in the same script, so this is a low-risk gap, but it should be spot-checked once sudo access is available.
- **Found and fixed during live verification** (not part of the original plan): seven `HardwareTest` subclasses (`GantryTestBase`, `VisionDetailBase`, and 5 standalone `__init__` overrides across `test_board_calibration.py`, `test_magnet.py`, `test_clock_integration.py`, `test_square_navigation.py`, `test_manual_gantry.py`) had `__init__(self, gpio_interface=None, display_interface=None)` signatures that didn't forward the new `cancel_check` kwarg to `super().__init__()`, so instantiating almost any real registered test (`GantryFullTest`, `BoardCalibrationTest`, `CameraCornerDetectionTest`, etc.) via `run_test()` raised `TypeError`. Caught by actually running `--all --mock` rather than trusting a read-through; fixed in commit `9ff54a0`.
- **GPIO pin interlock**: new `chess_hw_interface/gpio_lock.py`, a non-blocking `flock()`-based mutex (`/tmp/.chess_gantry_pins.lock`). `stepper_driver_node`/`homing_node` each acquire a **shared** lock for their lifetime (they already coordinate via ROS and are meant to coexist); `test_raw_motor`/`test_timing_sweep` (the only two hardware tests that open their own independent pigpio connection to the identical BCM pins) must acquire an **exclusive** lock before touching the motors, refusing to start with a clear error if a production node is live. Verified directly: a standalone two-process-style script confirmed shared+shared succeeds, shared+exclusive fails, and exclusive+exclusive fails, exactly matching the intended semantics — this logic is pure `fcntl`/`os` with no pigpio/ROS dependency so it could be exercised precisely.
  - **Update (real end-to-end confirmation, `pigpiod` running)**: with `stepper_driver_node` and `homing_node` both live against real pigpio, ran `python3 -m chess_hw_interface.testing.test_runner --category gantry --subtest raw_motor` — it correctly refused to start with `Setup failed: gantry GPIO pins are in use by a running production node (stepper_driver_node/homing_node). Stop the live ROS stack before running this test.`, failing before `PigpioStepper()` was ever constructed and before any pin was touched. This is the interlock's actual intended real-world scenario, fully confirmed.
- **Noted but out of scope for this phase**: running `--all --mock` (independent of any cancellation) never completed within 30s in this environment — some non-mocked test path (likely a `gantry`/`vision` test that spins up a real ROS node regardless of `--mock`) appears to hang without live hardware/production nodes present. Pre-existing, unrelated to this phase's changes (mock mode only swaps `gpio_interface`/`display_interface`, not the real `rclpy.Node` subclasses several tests construct internally). Also independently reproduced a `terminate called without an active exception` C++-level abort/core-dump at `test_runner.py`'s process exit in both the mock run and the real-pigpiod `raw_motor` rejection test above — happens *after* the test's own result is already printed, during `rclpy.shutdown()`/`monitor_node.destroy_node()` cleanup, most likely a race between the daemon `monitor_thread` (still spinning `RosMonitorNode`) and shutdown. Pre-existing, not a regression from this phase's changes, doesn't affect the CLI's printed result — but worth a proper fix during Phase 4 or 5 since it means every CLI test invocation currently core-dumps on exit.

### Phase 4 — First live-motor session + remaining gantry findings
Homing/jog/square-nav/pick-and-place + e-stop-during-motion, castling stale-snapshot fix, corner-BFS-can't-start-from-blocked-source fix, physical measurement of `x_max_mm`/`board_origin_x/y_mm`. (Graveyard slot-counter reset — originally scoped here — was pulled forward and done in Phase 9h, since it turned out to need zero new plumbing: `/motion/abort` was already published at every game-ending/reset moment.) See `.agent/workflows/hardware-test.md`'s "Phase 4 Checklist" section for the exact runnable commands.

### Phase 5 — `chess_perception`
Fallback camera never publishes `CompressedImage`, dead `premove_avg_count` averaging, corner-label cross-frame identity drift, stale-corner republish with no staleness marker, unpopulated `BoardState.msg` fields + stale hardware-test service references.

**Status: ✅ DONE, live-verified on the Pi 2026-07-09.** The fix itself was implemented and committed (`6a66588`) the previous session; this session's job was Pi-side verification (the previous session ended before that happened — `IMPLEMENTATION_PLAN.md`/`PROJECT_STATUS.md` hadn't been updated and the Pi's `chess_interfaces`/`chess_perception`/`chess_ui` install trees still predated the commit).

- **Pre-verification housekeeping**: `git pull` on the Pi confirmed already at `6a66588`. Rebuilt `chess_interfaces` (required — `BoardState.msg` field removal needs CMake message regeneration, symlink-install doesn't cover `.msg` changes) plus `chess_perception`/`chess_ui`/`chess_logic`/`gantry_control`/`chess_hw_interface`, all clean. The running production nodes (`board_detector_node`, `piece_detector_node`, `chess_os`/`chess_ui`, `test_runner_node`) all predated the commit (started 14:31–21:25 vs. the 23:19 commit) — restarted all three process groups with the user's confirmation. Hit and fixed a self-inflicted snag: killing the stale `perception_launch.py` process tree didn't include its `camera_node` (camera_ros) child, orphaning it and blocking the fresh relaunch from acquiring the CSI camera (`failed to acquire camera`); killed the orphan and relaunched cleanly.
- **`BoardState.msg` field removal**: confirmed via `ros2 interface show chess_interfaces/msg/BoardState` — `pieces`/`fen` fields gone, only `header` + `corners` remain.
- **Camera fallback `CompressedImage` publish**: the Pi's production camera backend is `camera_ros` (already publishes compressed), not our Python fallback, so this needed a targeted test — instantiated the real `CameraNode` class with topics remapped to `/test/...` and `use_picamera2:=false`/`camera_id:=99` (guaranteed no real backend, never touches the live CSI camera), then called its real `_publish_frame()` directly with a synthetic frame. Confirmed a genuine `CompressedImage` (jpeg, decodes back to a valid image) and the existing raw `Image` both arrive on the remapped topics.
- **`premove_avg_count` real averaging**: instantiated the real `PieceDetectorNode` with topics remapped and `premove_avg_count:=3`, drove it with a synthetic corner message + a stream of distinct synthetic frames, called `capture_premove`, and watched `/perception/reference_status` progress `1/3 → 2/3 → 3/3 (averaged)` across separate detection ticks — confirms real cross-tick accumulation (old dead code finalized on frame 1 regardless of the configured count).
- **Corner-label nearest-neighbor anchoring**: the physical camera currently frames only ~4 squares (pre-existing calibration gap, not this phase's concern — see Known Issues #2), so board corners are never detected live. Verified instead by calling the actual installed `BoardDetector._order_corners()`/`_order_corners_geometric()` directly with synthetic near-symmetric (diamond-ish) corner sets: for the identical frame-2 detected point set, the anchored path reproduced frame 1's established TL/TR/BR/BL identity while the old unanchored geometric heuristic flipped it — the exact bug the audit described, reproduced and confirmed fixed on the real shipped code. Also confirmed the drift-guard: an implausible/far-away `previous` correctly falls back to the geometric heuristic instead of forcing a bogus match.
- **Hardware-test catalogue fixes**: `python3 -m chess_hw_interface.testing.test_runner --list` confirms `vision/pieces` and `vision/fen` are gone (only `full`/`corners`/`board`/`squares` remain). Ran `vision/full` and `vision/corners` directly through the real `base_test.py`/`HardwareTest` machinery with `--mock` (bypasses `run_hw_test.sh`'s sudo requirement, which is still unavailable non-interactively in this environment — same pre-existing gap as Phase 1/3, unrelated to this phase) — both ran cleanly with correct service references (`/perception/capture_premove`), no `AttributeError`/stale-service crashes. `vision/full`'s "Manual Capture Service" step failed because `camera_ros` (the Pi's production backend) never implements `/camera/capture` — a pre-existing gap unrelated to this commit, not fixed here. `vision/corners` initially failed at "no board corners detected" due to the camera-framing bug below — **since fixed** (see next entry), re-verified working live.
- **`perception_test_launch.py` deletion**: confirmed gone from `src/chess_perception/launch/` on the Pi; `git grep` found no remaining references anywhere in the tree.
- All test/verification processes and synthetic topic publishes were cleaned up afterward (remapped `/test/...` topics never touched production topics); production nodes left running on the rebuilt code.

**Follow-up bug found and fixed during this verification session (2026-07-09, not in the original audit): camera severely cropped, board never fully in frame.** While testing corner detection, found the physical camera view covered only ~4 squares regardless of physical aiming. Root cause: `camera_ros`'s `sensor_mode` parameter was never set in `perception_launch.py`, so libcamera's own mode-selection heuristic picked a raw sensor mode independently of the requested `width`/`height` — for this sensor+role+size combination it silently chose a dead-centered ~39%×39% crop of the full 3280×2464 sensor array (`ros2 param get /camera_node ScalerCrop` defaulted to `{(1000,752)/1280x960}`). This is not a ROS/camera limitation — it's the exact bug our own (unused) Python fallback backend's `_try_picamera2()` already had a fix + comment for (`sensor={'output_size': (1640, 1232)}`), just never applied to the `camera_ros` backend actually in production. Fixed by adding `'sensor_mode': '1640:1232'` (the sensor's native 2×2-binned full-FOV raw mode) to `camera_ros`'s launch parameters in `perception_launch.py` — a one-line, ROS-native launch-parameter fix, no scripts/workarounds. Live-verified on the Pi: `ScalerCrop` now defaults to `{(0,0)/3280x2460}` (the whole array); a snapshot through the full production pipeline shows the complete 8×8 board for the first time; `/perception/board_geometry` now publishes real detected corners (previously never fired at all); the launch file's auto-capture-premove sequence succeeded (`Pre-move reference captured`) for the first time instead of its previous permanent `No image or board corners available` failure. Commit `1eec875`.

### Phase 6 — `chess_ui`
E-stop-clear doesn't reset hardware latches, calibration/settings pushes have zero runtime effect until node restart, SSE test-feedback queue not cleared between runs, jog has no timeout, calibration push silently dropped if service not ready, minor TOCTOU/bare-except cleanup.

**Status: ✅ DONE, live-verified on the Pi 2026-07-09** (same session as Phase 5; see the rebuild/restart notes above — `chess_ui` was already running the rebuilt code from that point on). All verification below was driven through the actual live `chess_ui` Flask server on port 5000 via `curl`, exactly as a browser would, or via the real installed node classes — no jog/motion commands were sent to real hardware (gantry power is off this session; jog-related verification used an isolated, ROS-disconnected process instead, see below).

- **E-stop clear**: launched `stepper_driver_node` standalone (`pigpiod` up, gantry power off — GPIO writes with no motor power are harmless, matches the pattern from Phase 3's connectivity checks). `POST /api/hw/estop` → node's own log: `EMERGENCY STOP triggered`. `POST /api/hw/estop/clear` → node's own log: `Emergency stop cleared`. Confirms the endpoint genuinely toggles the hardware-side latch via `/emergency_stop`, not just the local UI flag.
- **Runtime param-callback (live-reconfigure)**: launched `motion_planner_node` standalone, `ros2 param set /motion_planner_node board_origin_x_mm 42.5` → node's own log went from `a1=(20.0, 20.0)mm` (startup default) to `a1=(42.5, 20.0)mm` — confirms the node's cached attribute, not just parameter-server bookkeeping, updates live. Same for `game_manager_node`: `ros2 param set /game_manager_node engine_think_time_s 3.75` → log `engine_think_time_s updated to 3.75s`.
- **Retry-when-service-not-ready**: with `motion_planner_node` stopped, published a synthetic `/gantry/pose` (state-reporting only, commands nothing) to give `chess_ui` a non-zero position, drove `save_a1`/`save_h8`/`calibration/apply` through the real API (`board_origin_x_mm=0.0`, `square_size_mm=30.0`, differing from the node's eventual startup defaults) while the target node didn't exist yet, then started `motion_planner_node` fresh. Its log showed the queued push land within milliseconds of the `SetParameters` service becoming ready (`a1=(20.0,20.0)` → `(0.0,20.0)` → `(0.0,0.0)`, `sq=25.0` → `30.0`) — confirms the push was retried rather than silently dropped despite arriving before the node existed. Cleared the test calibration and reset the synthetic pose to `(0,0)` afterward.
- **SSE feedback-queue drain**: through the real API, started `vision/corners` and let it finish (fails on the pre-existing sudo gap, see Phase 5 notes) without ever opening `/api/tests/stream` (simulating a disconnected browser tab, leaving an unconsumed result in the queue). Started a second run (`vision/board`) and immediately opened a fresh `/api/tests/stream` connection — the very first line received was `Sourcing ROS 2 humble underlay...`, the genuine start of run #2, with zero leftover from run #1. Confirms the queue is genuinely drained at the start of each run.
- **Jog watchdog**: gantry jogging through the live endpoint was correctly blocked by the permission layer as physical-motion territory (Phase 4 scope, skipped this session per the user). Verified the watchdog logic in isolation instead: a throwaway process that imports the real `chess_ui.state`/`chess_ui.ros_client.jog_loop` but never starts a ROS node (`ros_node` stays `None`, so no `/stepper/velocity` publish ever reaches real hardware) — confirmed `state._jog["active"]` auto-flips to `False` ~0.8s (`JOG_WATCHDOG_TIMEOUT_S`) after the last heartbeat with no further refresh, and stays `True` indefinitely when heartbeats keep arriving every 100ms.
- **Bare-except logging**: verified by code inspection only (trivial `pass` → `self.get_logger().debug(...)` change in `_on_diff_img`/square-scores callbacks); not separately live-tested.
- All test-only node instances (`stepper_driver_node`, `motion_planner_node`, `game_manager_node`) were stopped afterward; production stack left exactly as restarted for Phase 5 verification.

### Phase 7 — Corner-visibility + manual-override UI
Folded into Phase 8e below (same `/perception/board_debug` plumbing, built together). `board_detector_node` already publishes an annotated corner-overlay debug image that `chess_ui` never displays; the original manual drag-corners feature (removed 2026-07-08 during the Chess OS consolidation on the premise that automatic detection made it redundant — a premise broken by the camera-framing bug, see Phase 5's follow-up entry) needs a proper home back in `chess_ui` as a correction path, not primary input, now that automatic detection genuinely works.

### Phase 8 — Vision robustness overhaul (piece-move detection)
Full context, root-cause analysis, and phase-by-phase design in `/Users/ianfuller/.claude/plans/transient-hopping-grove.md` (the approved plan for this initiative). Summary of the four root causes found: (1) continuous auto-exposure/AWB causes board-wide brightness/color drift between reference and post-move captures, easily mistaken for "the camera keeps moving the image"; (2) `game_manager_node`'s post-move capture calls `/camera/capture`, which `camera_ros` (the production backend) doesn't implement, so it's actually just grabbing whatever `piece_detector_node`'s independent 2Hz timer publishes next — no "wait until settled" concept exists; (3) perspective bleed on far ranks because pieces (objects with height) don't sit flush with the homography's board plane, and today's per-square ROI is a uniform centered crop that doesn't exploit which part of a piece's silhouette actually projects correctly; (4) `_match_legal_move` works off a boolean thresholded set with only "drop exactly one square" noise tolerance, not the continuous scores it already has access to.

- **Phase 8a** — Lock camera exposure/white-balance (`AeEnable`/`AwbEnable` sample-then-lock via `camera_ros`'s existing ROS parameters) + chess_ui readout/lock/relock controls. Cheapest, likely highest-impact fix for "the image keeps moving."
- **Phase 8b** — Stability-gated post-move capture in `game_manager_node`: wait for K consecutive identical `changed_squares` reads (bounded timeout + mode fallback) instead of accepting whatever the next tick reports, extending the same idea `premove_avg_count` already does on the reference side.
- **Phase 8c** — Perspective-aware per-square ROI (biased toward the near-camera edge, rank-dependent) + a second edge/structure-diff channel alongside today's LAB color diff, both scoped per-square (not whole-board) to avoid the "full board is a mess of edges" problem.
- **Phase 8d** — Confidence-scored legal-move matching in `game_manager_node`: score every legal move by its footprint's diff scores instead of exact-match-then-drop-one-square; surface top-3 candidates + a manual override in the UI.
- **Phase 8e** — Remaining developer tuning UI (ties in Phase 7's corner overlay/manual-override + a turn-by-turn tuning log).

**Status: 🔄 IN PROGRESS as of 2026-07-09.** Plan approved by the user same session as the camera-framing fix (Phase 5 follow-up) that unblocked automatic corner detection in the first place.

#### Phase 8a — Lock camera exposure/white-balance
**Status: ✅ DONE, live-verified on the Pi 2026-07-09.** Commit `bdd5988`.

Design adjustment made during implementation: the plan's original "sample current AE-converged values, then pin them explicitly" isn't achievable as written — `camera_ros`'s `ExposureTime`/`AnalogueGain`/`ColourGains` parameters report `Parameter not set` until explicitly written at least once, with no mechanism to read back the AE algorithm's live-computed value (confirmed via repeated `ros2 param get` over several seconds while AE was actively converging — always `not set`; also no per-frame metadata topic exists). Separately, `ExposureTime`'s declared parameter range is broken (`{75}..{0}}`, an inverted/degenerate range, likely because `FrameDurationLimits` was never set) so it can't even be explicitly written to a chosen value right now. Given this, implemented the simpler and equally effective mechanism: just toggle `AeEnable`/`AwbEnable` off — this freezes the sensor at whatever exposure/gain/white-balance it had converged to at the moment of disabling (confirmed empirically, see below), without needing to read back or explicitly set specific values at all.

- Added `chess_ui` Perception-tab toggle ("Lock exposure / white-balance"), wired through the existing `SetParameters`-push pattern (`_pending_camera_params`/`_check_pending`, same retry-until-ready mechanism Phase 6 added for `motion_planner_node`/`game_manager_node`) to a new `/camera_node/set_parameters` client.
- **Live before/after verification on the Pi**, same untouched physical board state, via `/perception/square_scores`:
  - **Unlocked** (`AeEnable`/`AwbEnable` = true, continuous auto): min=2.8, **max=121.1**, mean=**27.7** — already above the 18.0 default `diff_threshold` on average, with **33 of 64 squares** scoring above threshold despite nothing being touched. This alone would falsely flag roughly half the board as "changed" on any given tick.
  - **Locked** (`AeEnable`/`AwbEnable` = false via the new toggle): min=2.6, max=8.3, mean=4.85, **0 of 64 squares** above threshold.
  - Confirmed via `ros2 param get /camera_node AeEnable`/`AwbEnable` that the toggle genuinely changes the real hardware-facing parameter (not just local UI state), and via `/api/status`'s `exposure_locked` field that it's correctly reflected back.
- **Left locked** as the Pi's running state after this verification (recommended default going forward) — matches the toggle's own default-off `_state` value only until the operator/next session re-enables it; the running `camera_node` process itself keeps whatever was last set until restarted (it doesn't reset to the declared default automatically).
- **Not fixed, out of scope**: `ExposureTime`'s broken parameter range (would require setting `FrameDurationLimits` first and/or patching `camera_ros` itself, an external package) — not needed since disabling auto alone achieves the goal.

#### Phase 8b — Stability-gated post-move capture
**Status: ✅ DONE, live-verified on the Pi 2026-07-09.** Commit `3e1ea98`.

`game_manager_node._do_capture_board()` no longer accepts whichever `/perception/changed_squares` reading arrives first after the clock press. `piece_detector_node` publishes on every ~0.5s detection tick regardless of game state, so the old code could latch onto a frame taken mid-motion or with a hand still over the board. Now waits for `capture_stability_count` (new param, default 3) consecutive identical readings before trusting the result, with `board_capture_timeout_s` (default raised 5.0→10.0 to give the stability wait room) as a bounded overall timeout; if it never fully stabilizes, falls back to the most-common (mode) reading across the window rather than failing outright — the same timeout-with-fallback pattern already used for homing retries in this node. New `/game_manager/capture_progress` topic (e.g. `"Stabilizing: 2/3 consistent reads"`) surfaced in `chess_ui`'s Perception tab.

Live-verified on the Pi using the real installed `GameManagerNode` class with `/perception/changed_squares`/`/camera/capture` remapped to test-only topics (never touched the production perception graph):
- **Stability path**: published a realistic noisy-then-settling sequence (`e2` → `d2` → `e2,e4` → `e2,e4` → `e2,e4`). Log: `Stable read after 5 tick(s): ['e2', 'e4']` — correctly ignored the first two noisy/partial readings and only accepted the result once it repeated 3 times in a row, exactly the scenario ("hand still on the board") this phase targets.
- **Mode-fallback path**: published a sequence that never produces 3-in-a-row (`e2,e4`/`d2,d4`/`e2,e4`/`a1`/`e2,e4`/`b1`/`e2,e4` — majority reading 4/7 but never consecutive), with `board_capture_timeout_s:=4.0` for a fast test. Log: `No stable read within 4.0s — using most-common reading (4/7 ticks agreed): ['e2', 'e4']` — correctly recovered the right answer instead of hanging or failing outright.
- Both tests' final `node._latest_changed_squares` matched the expected `{e2, e4}` exactly. Cleaned up afterward; production stack (`camera_node`/`board_detector_node`/`piece_detector_node`/`chess_os`/`test_runner_node`) confirmed untouched throughout.

#### Phase 8c — Perspective-aware ROI + multi-modal (color + structure) scoring
**Status: ✅ DONE (mechanism), live-verified on the Pi 2026-07-09 — real on-board perspective-bleed improvement still needs physical verification (see below).** Commit `3c1e427`.

Two independent additions to `piece_detector_node._compute_square_scores()`, both default-off (`0.0`) so existing behavior is unchanged until tuned:

- **`roi_bias_fraction`**: shifts each square's sampled vertical window (not its size) toward whichever edge of the warped image carries a piece's board-contact point — the only part of a piece's silhouette the homography maps correctly, since the rest "leans" due to the piece's height, worse on ranks farther from the camera. New `_square_roi_bounds(row, col)` helper (shared by scoring and the debug overlay) computes this, clamping the window to stay within its own square's row-band rather than ever reaching into a neighboring square — a large bias shrinks the sampled window instead of translating outside its bounds. **Deliberately left as a signed, tunable parameter (not a hardcoded direction)**: which physical edge is "near-camera" depends on the camera's mounting orientation relative to the board, which isn't reliably derivable from code alone, and guessing wrong would make bleed worse, not better. `piece_debug`'s debug image now draws the actual sampled ROI boxes (cyan) whenever a bias is active, so the direction/magnitude can be tuned empirically against the real board.
- **`edge_diff_weight`**: a second, independent evidence channel — Sobel gradient-magnitude (structure) diff, combined with the existing LAB color diff. Scoped per-square (not whole-board) to avoid the "full board is a mess of edges" problem.

Both exposed as sliders in `chess_ui`'s existing Detection Thresholds panel, pushed through the existing `/api/detector_params` → `SetParameters` path (Phase 6's mechanism).

**Live-verified on the Pi** (real installed `PieceDetectorNode`, remapped test-only topics, never touched the live camera):
- **ROI bias math**: a small in-margin bias (below the ROI's own margin slack) translates the window by the exact expected pixel amount with height fully preserved; a large bias (0.4) correctly clamps to stay within its own square's row-band (never reaches into a neighbor), including at the board's own edge rows.
- **Edge/structure channel's lighting robustness** — the core claim of this addition: on a pure uniform +30 brightness shift applied to an otherwise-identical synthetic image (no structural change at all — exactly Phase 8a's exposure-drift scenario), the edge channel contributed **+0.00** to the worst-case square score. On the same base image with a real structural change (a drawn rectangle), the edge channel contributed **+101.16** to that square's score. This is about as clean a demonstration as possible that it's a genuinely more lighting-robust signal than raw color diff, not just color diff computed twice.
- Confirmed via the real API that both new params push through to the live node (`ros2 param get /piece_detector_node roi_bias_fraction/edge_diff_weight` reflected `0.15`/`0.5` after a `/api/detector_params` POST, then reset to `0.0`/`0.0`), and that default (`0.0`/`0.0`) behavior is unchanged from before this phase (fresh `/perception/square_scores` after restart: mean 5.95, max 12.5 — consistent with Phase 8a's locked-exposure baseline, no regression).

**Not yet verified — needs the user's physical participation**: whether `roi_bias_fraction` actually reduces real perspective bleed on far ranks requires moving physical pieces there and comparing flagged-square accuracy before/after tuning the slider, per this phase's own design (empirical tuning against the real board, not a hardcoded direction). This is the one piece of Phase 8 that can't be verified by the agent alone.

#### Phase 8d — Confidence-scored legal-move matching
**Status: ✅ DONE, live-verified on the Pi 2026-07-09.** Commit `c9fc5e1`.

`game_manager_node._match_legal_move()` previously worked off the boolean-thresholded changed-squares set: exact-match a legal move's footprint, and on failure, try dropping exactly one extra square as noise tolerance. That could handle either a missed detection *or* a false positive, but not both at once, and didn't use the continuous diff scores `piece_detector_node` already publishes on `/perception/square_scores`.

Replaced with `_move_match_score()`: for every legal move, sum the scores on its footprint squares (from `_get_move_changed_squares()`, unchanged) minus every *other* square still elevated above `match_noise_floor` (new param, default 8.0) and left unexplained by that move. The highest-scoring legal move overall wins, instead of pattern-matching a single thresholded set — naturally tolerant of missed detections, false positives, and perspective bleed simultaneously, since it's a global comparison across every legal move rather than a single exact-match-then-patch attempt. `game_manager_node` now also subscribes to `/perception/square_scores`, snapshotting it into `_captured_square_scores` at the exact moment Phase 8b's stability gate accepts a result (`_do_capture_board()`). New `min_confident_match_score` (default 15.0) guards against confidently picking an arbitrary "least-bad" legal move when nothing really explains the observed scores (e.g. a spurious clock press with no real move made). New `/game_manager/move_candidates` (top-3 scored moves) surfaced in `chess_ui`'s Perception tab for transparency into why a move was inferred.

Note: not summing raw scores over the whole board for the "unexplained" penalty (only counting squares above the noise floor) was a deliberate design choice made during implementation — summing every non-footprint square's raw score would otherwise bias against small-footprint (2-square) moves relative to 4-square castling moves purely from having a larger complement set, even under Phase 8a's low, roughly uniform background noise level.

**Live-verified on the Pi** (real installed `GameManagerNode`, `/perception/changed_squares`+`/perception/square_scores`+`/camera/capture` remapped to test-only topics, never touched the production perception graph):
- **Correct move despite unrelated noise**: synthetic scores with `e2`/`e4` high (150 each, a real e2e4 move) *plus* an unrelated elevated `d5` (40, simulating hand-shadow/perspective bleed that doesn't belong to any move's footprint). Log: `Top candidate moves: e2e4=268.0, e2e3=-20.8, c2c3=-304.6` → `Inferred move: e2e4`. The correct move won by a wide, clearly-separated margin despite the extra noise.
- **Declines to guess on pure noise**: synthetic scores with only low-level background noise everywhere (nothing resembling a real move). Log: `Top candidate moves: c2c3=11.4, g1h3=10.5, a2a3=10.5` → `No confident move match (best=c2c3 score=11.4 < min=15.0)` — correctly returned `None` instead of confidently picking an arbitrary legal move.
- Production stack (`camera_node`/`board_detector_node`/`piece_detector_node`/`chess_os`/`test_runner_node`) confirmed untouched throughout; `chess_ui` restarted afterward to pick up the new `move_candidates` subscription/display (has nothing to show yet since `game_manager_node` isn't running in production this session — Phase 4 prerequisite).

#### Phase 8e — Remaining developer tuning UI + corner-visibility/manual-override tie-in (Phase 7)
**Status: ✅ DONE (core), live-verified on the Pi 2026-07-09 — turn-by-turn tuning log deferred (see below).** Commit `158c356`.

Two additions, both in `chess_ui`'s Perception tab:

- **Corner-overlay visibility**: `board_detector_node` already publishes an annotated corner-detection debug image (`/perception/board_debug` — TL/TR/BR/BL markers + board outline) that `chess_ui` never displayed anywhere. New `/api/corner_frame` route + live image in a new "Board Corners" card, refreshed on the same 1.5s interval as the existing piece-diff heatmap.
- **Manual corner-drag override** (the Phase 7 item, folded in here): restores the manual-corner-calibration workflow removed during the 2026-07-08 Chess OS consolidation on the premise that automatic detection made it redundant — a premise broken by the camera-framing bug fixed earlier this session (automatic detection had been silently broken by it the whole time, with no fallback available). `board_detector_node` now subscribes to `/perception/manual_corners_override` (`geometry_msgs/Polygon`, 4 points in TL/TR/BR/BL order, full-resolution pixel coordinates) — when set, `_detect_tick()` uses it directly instead of running automatic detection that tick (constructing a real `BoardGeometry` via `cv2.getPerspectiveTransform`, so downstream consumers see no difference from an automatic detection); an empty `Polygon` clears the override and resumes automatic detection. Deliberately a **correction path, not the primary input** — automatic detection stays the default. `chess_ui` gets a draggable-corner canvas overlaid on a raw camera snapshot (tracks native image pixel coordinates internally, independent of CSS display scaling, so dragging stays accurate regardless of window size) plus Apply/Clear buttons and a live status indicator.

**Live-verified on the Pi** through the real running system (curl, as the UI would):
- Pushed a manual override via `/api/perception/manual_corners` — `/perception/board_geometry` immediately reflected the exact pushed coordinates (`(50,50)/(590,50)/(590,430)/(50,430)`), confirmed via `ros2 topic echo`.
- Cleared it via `/api/perception/manual_corners/clear` — `board_geometry` immediately reverted to genuine automatic detection (different, freshly-computed corner values), confirmed automatic detection was really running again, not just frozen on the last manual value.
- **Found a real, live bug while testing this** (not caused by this phase — pre-existing, now visible for the first time thanks to the new corner-overlay): with automatic detection active, `/api/corner_frame` sometimes shows the detector locking onto a strong *internal* sub-rectangle of the grid (roughly the board's right half) instead of the true outer board boundary, rather than "no board detected" or the full 8×8 outline. Demonstrated the manual override genuinely fixing this live: pushed corners eyeballed from the actual full-board extent visible in that exact frame, and `/api/corner_frame` immediately showed the correct full-board outline instead. This is direct, concrete evidence of why this phase's fallback matters, discovered *by* the visibility feature this phase adds. Not fixed (that's automatic-detection accuracy tuning, arguably parked under Phase 8c/a general "corner detection accuracy" bucket, not scoped in the original Phase 8 plan) — logged as a new finding instead.
- Cleared the test override afterward (a rough eyeballed estimate, not a proper drag-calibration) so production isn't left on an unofficial hand-picked value — left on genuine automatic detection, matching the phase's own "correction path, not primary input" design intent. A real calibration via the new drag UI is up to the user whenever convenient.

**Deferred, not implemented this session**: the turn-by-turn tuning log (recent N turns' raw per-channel scores + inferred move + confidence, retained in `chess_ui` state for post-hoc review) from the original plan. Lower priority than the corner-visibility/override work above given session time, and less urgent now that `/game_manager/move_candidates` (Phase 8d) already surfaces per-turn reasoning live — a persistent log adds convenience, not new capability. Can be picked up in a future session if still wanted.

---

## Phase 9 — Initiative: Path to a Complete, Auto-Starting Real Game

**Trigger**: after this session's perception validation closed out Phase 7/8e's last open item (manual corner calibration confirmed pixel-accurate against the real physical board with all pieces set up; exposure lock confirmed engaged; a fresh premove reference gave a quiet baseline — max diff 3.1, mean 1.9, threshold 18), the user asked for a full step-back: an easy way to launch/auto-start everything, a complete gap list for a real game, whether a new audit is needed, a hardware-validation command list, UI gaps, and specifically a manual "advanced controls" board-editing backup for misreads.

Two of the user's concerns turned out to already be solved (confirmed via code reading, not assumption) and didn't need any work:
- **Graphical board with piece images** already existed via `chessboard.js` + piece images (`index.html`) — not a gap. (Its `pieceTheme` URL turned out to be silently 404ing the whole time — see the standalone fix below, found and fixed in the same session before Phase 9 proper started.)
- **"Board state shouldn't flicker every frame"** was already solved by `game_manager_node`'s stability gate (Phase 8b, 3 consecutive identical reads) and confidence-scored matching (Phase 8d) — not a gap.

Everything else below was a confirmed real gap or bug, each traced to specific code before implementing (see the approved plan at the time, `robust-fluttering-cupcake.md`). Full plan approved by the user in one pass; phases 9a-9h executed and live-verified in order in the same session.

### Standalone fix (same session, before Phase 9 proper) — chessboard.js piece images 404
`pieceTheme` pointed at `unpkg.com/@chrisoakman/chessboardjs@1.0.0/img/...` — confirmed via direct `curl` that this 404s, because that npm package's published tarball only ships `dist/{js,css}`, no `img/` directory at all. Every one of the 32 piece images had been failing to load since the chessboard.js integration was first added; visually this showed as flickering broken-image icons on every ~500ms status poll re-render (the user's report of "flickering placeholders"). Fixed by switching to `chessboardjs.com/img/chesspieces/wikipedia/{piece}.png` — the library's own canonical asset host, confirmed reachable both locally and from the Pi. Commit `9944b6d`. Live-verified: `curl`'d the served page and confirmed the corrected URL is what's actually shipped.

### Phase 9a — Reconcile `full_system_launch.py`/`perception_launch.py` divergence
**Finding**: the perception stack actually running and validated all session was started via `perception_launch.py` (`use_camera_ros:=True`, `sensor_mode:'1640:1232'` — the camera-framing fix from Phase 5's follow-up). But `full_system_launch.py` — the one-file "launch everything" entry point — hand-rolled a *different* camera config (`chess_perception`'s own Python `camera_node`, 1280x720, no `sensor_mode`). Running "the one launch file" would not have reproduced the session's proven-good state.

**Fix**: replaced `full_system_launch.py`'s inline Layer 3 with an `IncludeLaunchDescription` of `perception_launch.py`, forwarding `use_camera_ros`/`use_picamera2`/`calibration_file`, so one file is now the single source of truth for the camera layer. Commit `eb71d12`.

**Verified**: a safe dry-run (`ros2 launch ... --show-args`) on the Pi — this only parses/builds the `LaunchDescription` tree, starts no processes — confirmed the include resolves cleanly and the correct camera defaults (`use_camera_ros:'True'`, `width:'1640'`, `height:'1232'`) now flow through from the included file. Did not run the full launch file live (would start Layer 1/2 hardware+gantry alongside the already-running production perception stack, and `game_manager_node`'s loop calls `_do_home()` immediately — real motor movement, correctly out of scope without the user physically present per Phase 4).

### Phase 9b — Game-state persistence/resume
New `game_state_file` param (default `~/.chess/game_state.json`) on `game_manager_node`. Atomically (`os.replace`) snapshots FEN/pre-move-FEN/move-history/clock-times from the existing call-sites that already mutate+republish the board (human move accepted, computer move accepted) — no new timer/thread. Loaded once at startup (`_load_persisted_state()`, called after homing/premove-capture, before the main loop); only resumes a non-`IDLE`/`GAME_OVER` snapshot, and always resumes into `WAITING_PLAYER_MOVE` regardless of the exact state at crash time (never mid-motion/mid-capture — hardware position can't be trusted after a crash). Sets `_resumed_pending_ack`, which blocks the next clock press (`_on_clock_hit`/`_svc_start_game` both gate on it) until a new `/game/ack_resume` service confirms the operator has physically verified the board — surfaced in `chess_ui` as a banner requiring explicit acknowledgment. New game / real game-over both clear the persisted snapshot.

Clock-time restore is conservative by design: only pushed via a new `/clock/set_times` service (`SetClockTimes.srv`, on `chess_clock_node`) if the live clock still looks freshly-started (within 1s of the configured `time_per_player_s` default, now also threaded through to `game_manager_node` for this comparison) — otherwise skipped with a warning, so a `game_manager_node`-only crash can't wrongly roll back an already-correct live clock that `chess_clock_node` itself never lost track of. Commit `1c1c72f`.

**Live-verified** with a throwaway `GameManagerNode` instance (background homing-retry loop harmless — `/gantry/home` unavailable, no motor call ever attempted) and a real `chess_clock_node` instance: 4/4 persistence assertions (nothing-to-load, save+load round-trip with correct FEN/history, IDLE/GAME_OVER never resumed, clear removes the file) plus a live `/clock/set_times` service call confirmed via both the service response and a live `/clock/white_time` topic echo (`data: 123.0` matching the pushed value exactly).

### Phase 9c — Autostart-on-boot
New `setup/smart-chess.service` (git-tracked template, same convention as `setup/smart-chess-hw-tests.sudoers` — manually installed via `sudo cp`+`systemctl enable`, never silently auto-applied). `Requires=pigpiod.service`, `Restart=on-failure`, `StartLimitIntervalSec=60`/`StartLimitBurst=5` so a persistently-broken boot doesn't crash-loop forever. `deploy.md`'s "Auto-Start on Boot" section now points at the tracked file instead of a copy-paste heredoc.

Added an opt-in `respawn` launch arg (default `False`) to `perception_launch.py` (camera/board/piece nodes) and `full_system_launch.py` (`chess_ui`) — **first attempt used a manual `RegisterEventHandler(OnProcessExit(target_action=n, on_exit=[n]))` pattern reusing the same action object, which live-testing caught as broken**: it respawns once, then raises `"executed more than once"` on the second exit (confirmed via an isolated dummy-process launch file — fired correctly on exit #1, crashed on exit #2). Fixed by switching to `launch_ros`'s **native** `respawn`/`respawn_delay` kwargs on `Node`/`ExecuteProcess` (found in `launch/actions/execute_local.py` — not obvious from the constructor's own signature since it's inherited), confirmed via the same isolated dummy-process test to cycle repeatedly (3x in a 7s window) under `respawn:=True` and not at all under `respawn:=False`. Commits `55ef6b3` (broken version) → `97e022c` (native-kwarg fix, same session, caught before being left broken).

Deliberately scoped to the perception+UI layer only — **not** hardware/gantry nodes, since respawning those independently without a full re-home risks operating on stale position assumptions after a crash mid-motion; that layer is only recovered by the whole-unit systemd restart, which correctly re-homes on every restart.

**Verified**: both launch files' `--show-args` dry-run succeeded post-fix, confirming the `respawn` arg is exposed with the right default and description. The systemd unit itself was written and committed but **not installed/enabled on the Pi** — that's a persistent, boot-time system configuration change out of scope for an agent to apply unprompted; left for the user to install via the documented `sudo cp`/`systemctl enable` steps whenever they want autostart live.

### Phase 9d — Manual game-state override ("Advanced" tab)
**Root cause of the pre-existing brokenness**: `POST /api/fen` only ever wrote `chess_ui`'s own local display dict (`state._state["game_fen"]`) — never `game_manager_node`'s authoritative `self._board` — and was silently clobbered within ~2s by `game_manager_node`'s own periodic FEN republish timer. There was no way to correct a misread board without a full `/game/new_game` reset, discarding the whole game.

**Fix**: new `chess_interfaces/srv/ManualEdit.srv` (`string fen` → `bool success, string message`) exposed as `/game/manual_edit` on `game_manager_node`, following the exact validate-in-callback/apply-in-loop-thread pattern already used for `/game/resign`/`/game/new_game`: `chess.Board(fen)` parse + `board.is_valid()` check happen synchronously in the service callback (so the HTTP caller gets an immediate, accurate result), the actual `self._board` mutation happens only at the top of the next game-loop iteration via a new `_manual_edit_event`. Allowed from `IDLE`/`WAITING_PLAYER_MOVE`/`MOTION_ERROR`/`GAME_OVER` — this also gives `MOTION_ERROR` a real recovery path (correct the board, keep playing the same game) instead of only a full reset. FEN-replacement only for v1, not structured move/capture/castling actions — matches what a drag-and-drop editor naturally produces; castling rights/en-passant are conservatively cleared (`w - - 0 1` suffix), documented as a known limitation directly in the UI copy. `chess_ui` gets a new "Advanced" tab: a third `chessboard.js` instance in edit mode (`draggable`, `sparePieces`, `dropOffBoard:'trash'` — same library already on the page). The old `/api/fen` route now forwards to the new path instead of silently no-op'ing. Commit `f4f4f72`.

**Live-verified** with a throwaway `GameManagerNode` (homing bypassed via `node._do_home = lambda: True`, the established stub pattern for isolating game logic from hardware): malformed-FEN rejection (`chess.Board()` parse error surfaced verbatim), illegal-position rejection (`kk6/.../KK6` → `status()=4`, `STATUS_TOO_MANY_KINGS`), successful application with correct `EDIT:`-tagged move-history entry and state transition to `WAITING_PLAYER_MOVE`, and state-gating rejection from `CAPTURING_BOARD`. Also verified end-to-end through the live production `chess_ui` via `curl` (both the new `/api/game/manual_edit` route and the old `/api/fen` alias correctly report `"Service not available"` — an honest failure, unlike the old route's silent-success lie — since `game_manager_node` isn't running in production this session, a Phase 4 prerequisite).

### Phase 9e — Promotion choice UI
Two distinct sub-problems traced from the actual game loop, not one:
1. **Computer promotes**: its UCI move already unambiguously specifies the piece — pure UI fix, read the actual piece from the last `move_history_uci` entry instead of a hardcoded "queen" string.
2. **Human promotes (the real gap)**: `_do_validate_move()` always resolves promotion ambiguity to queen (all 4 promotion move-types share an identical vision footprint) **and pushes that move immediately**, before `PROMOTION_WAIT` even starts — a UI-only fix wasn't enough since the wrong move was already applied to `self._board`.

**Fix**: new `/game/set_promotion` service (`SetPromotion.srv`, `q`/`r`/`b`/`n`), valid only in `PROMOTION_WAIT` for a human promotion (new `_pending_promotion_is_human` flag, published on `/game_manager/promotion_is_human` so `chess_ui` can tell the two cases apart). Applied inside `_wait_promotion_confirm()`'s existing wait loop via `_apply_promotion_choice()`: pops the already-pushed default-queen move, reconstructs with the chosen piece, re-pushes, re-publishes FEN/history. `chess_ui`'s promo banner now shows Q/R/B/N buttons for a human promotion (Q pre-highlighted so players who don't care see no behavior change) or the correct piece name for a computer one. Commit `5cb5423`.

**Live-verified** with a throwaway node: state-gating (rejects outside `PROMOTION_WAIT`, rejects when the pending promotion is the computer's not the human's), invalid-piece-letter rejection, `_svc_set_promotion` correctly setting the pending piece + event, and `_apply_promotion_choice()` correctly swapping queen→rook (`a7a8q` → `a7a8r`, confirmed via both `move_history` and the actual piece type on the board) plus a no-op confirmation when the piece is already queen. 6/6 assertions passed. Verified live via production `chess_ui` that the new tab markup/JS (`promo-human-controls`, `promo-computer-piece`, `setPromotion`) are present in the served page.

### Phase 9f — UX polish bundle
Three independent, smaller gaps:
- **Invalid/rejected move**: new `/game_manager/move_rejected` topic, published whenever `_do_validate_move()` returns `None` (previously only a server log warning). `chess_ui` shows a transient banner, deliberately driven by the *client's own clock* (compares each poll's `move_rejected_at` to the last-seen value, then fades after 6s of browser-side `Date.now()`) rather than comparing directly against the Pi's timestamp, to sidestep any client/server clock-skew edge case.
- **Engine fallback visibility**: `RequestMove.srv` gains `bool used_fallback`, set by `chess_engine_node` whenever Stockfish wasn't used (unavailable, or the move request itself failed) — surfaced as a **persistent** (not transient — this is an ongoing integrity concern, not a one-off event) status pill in `chess_ui` whenever true. Also fires for `game_manager_node`'s own separate outer fallback (engine service entirely unreachable → first legal move).
- **Node health**: `routes.py`'s `EXPECTED_NODES` was missing `chess_clock_node` and `clock_servo_node` entirely — a crash of either was invisible in the UI's health panel. One-line fix.

Commit `e644b2f`. **Live-verified**: a throwaway node confirmed both new topics (`move_rejected`, `engine_used_fallback` True→False) fire and are received correctly; confirmed live via production `chess_ui` that the served page contains both new UI elements (`pill-engine-fallback`, `reject-banner`).

### Phase 9g — Draw-offer flow
New `/game/declare_draw` service, identical event-based pattern to `/game/resign` (`_draw_event`, checked at the top of the game loop, ends via the existing `_end_game()`). Deliberately scoped as a simple human-declared draw (functionally a softer resign variant) rather than a two-sided offer/accept negotiation with the engine — the computer has no independent judgment to consult beyond its own evaluation, so a real negotiation flow would add complexity with no obvious corresponding capability on the other side. New "Draw" button in `chess_ui`'s Game Controls card. Commit `7956b9b`.

**Live-verified** with a throwaway node: `/game/declare_draw` correctly transitions to `GAME_OVER` with reason `"Draw (declared by operator)"` (confirmed via both the service response and polling `node._state`). Note for future test-writing: forcing `_state` directly via a test harness (rather than letting it arise from the loop's own natural flow) collides with the same `_clock_hit_event.set()` "unblock any wait" convention `_svc_resign` already uses — the test's artificial state jump caused a one-cycle detour through a spurious board-capture/validate attempt before the outer loop got back around to the `_draw_event` check, delaying (not breaking) the transition by several seconds. This is a pre-existing characteristic shared with `/game/resign`, not a regression — production code never externally forces `_state`, so this timing quirk is a test-harness artifact, not a real one. Confirmed live via production `chess_ui` that the served page contains the new `confirmGameAct('draw', ...)` button wiring.

### Phase 9h — Cleanup
- `GraveyardManager.reset()` (defined, never called anywhere) — wired into `motion_planner_node._on_abort()`. Checked every `_abort_pub.publish()` call site in `game_manager_node.py` first (resign, declare_draw, new_game — all three, and only those three) to confirm every one is a game-ending-or-resetting moment, never mid-game, before wiring this in.
- Deleted the two stale, git-tracked `board_corners.json` files (repo root, `src/chess_perception/scripts/`) — confirmed read by no current production code (only by `fen_visualizer.py`, an orphaned standalone script not wired into any launch file or `setup.py` entry point).

Commit `dd0a246`. **Live-verified** with a real standalone `MotionPlannerNode` instance (pure ROS pub/sub, no GPIO/motor hardware needed to instantiate — confirmed via the project's own established reference notes): `_graveyard._next_slot` advanced 0→3 across three `get_next_slot()` calls, then a real `/motion/abort` publish reset it back to 0, with `_abort_requested` also correctly set.

### Non-code deliverables
- **Hardware-validation checklist**: added a "Phase 4 Checklist" section to `.agent/workflows/hardware-test.md` — the exact remaining runnable commands for first-live-motor validation, each explicitly marked as something to run directly or with the user present live, never scripted autonomously (matches this project's established practice after two prior sessions where the permission system correctly blocked an agent's unauthorized production-touching action).
- **Audit-necessity assessment**: no fresh full audit needed yet. Phases 1-8 already systematically remediated the 2026-07-08 audit with live verification at every step, and this Phase 9 step-back was itself a targeted audit of exactly the areas Phases 1-8 hadn't reached (deployment/launch layer, manual-override UX, promotion UX, persistence) — it already found and fixed 3 real bugs along the way (the launch-file camera divergence, the broken respawn-via-action-reuse attempt, the always-lying-about-success `/api/fen` route) using the same live-verify-everything discipline as every phase before it. The single biggest remaining source of genuine unknown-unknowns is Phase 4's first live-motor session — code that has never yet executed against real hardware motion. Recommend completing Phase 4 first; a fresh full audit makes more sense **after** that, since real motor movement is where genuinely new failure modes are most likely to surface, not in more static code re-reading.

---
*Created 2026-07-08 from the full audit. Update the status table and add session notes under each phase as work lands.*
